// Teams Filters - Stub Component
export function TeamsFilters({ filters: _filters, onChange: _onChange }: { filters: Record<string, unknown>; onChange: (filters: Record<string, unknown>) => void }) {
  return (
    <div className="bg-white p-4 rounded-lg shadow">
      <div className="text-gray-500">Filters placeholder</div>
    </div>
  );
}
